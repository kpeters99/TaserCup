#include "myTypes.h"
using namespace std;  

void swap(competitor_t* a, competitor_t* b);
int partition (competitor_t arr[], int low, int high);
void doQuickSort(competitor_t arr[], int low, int high);
